# Docker Image

### TAGS
Open5GS  : v2.3.6
UERANSIM : v3.2.4


### Make Help
```
$ make help
Common make targets :
  Tag Version
    Open5GS  : v2.3.6
    UERANSIM : v3.2.4

  Usage:
    make <target> [flags...]

  Command:
    help                               Show this help message.

    Build:
      build                            Build All Images.
      build-core                       Build Core Image.
      build-webui                      Build WebUI Image.
      build-ueransim                   Build UERANSIM Images.

    Clean:
      clean                            Remove All Images.
      clean-core                       Remove Core Image.
      clean-webui                      Remove WebUI Image.
      clean-ueransim                   Remove UERANSIM Image.

  Flags:
```

